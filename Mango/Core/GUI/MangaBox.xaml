<Window x:Class="Mango.Core.GUI.MangaBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        Title="MangaBox" Width="268" Height="413" Background="#00FFFFFF">
    <Grid Name="MainGrid" Width="268" Height="380">
        <Grid.Effect>
            <DropShadowEffect Direction="270" Opacity="0.2" ShadowDepth="3" />
        </Grid.Effect>
        <Border Background="White" />
        <Grid Height="378" VerticalAlignment="Top">
            <Image Name="Cover" Stretch="UniformToFill" Margin="0 -5 0 0"/>
            <Grid VerticalAlignment="Top" HorizontalAlignment="Center" Background="#2e1d1d1d" Margin="3">
                <Label Name="dbLabel" Content="MangaReader" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" FontStretch="3" />
            </Grid>
            <Grid Width="225" Height="31" VerticalAlignment="Bottom" Background="#6e1d1d1d" Grid.ColumnSpan="2" Margin="15,0">
                <Label Name="Title" Content="TitleHere" HorizontalAlignment="Center" Background="#00FFFFFF" Foreground="White" VerticalAlignment="Center" FontSize="16" FontStretch="3"/>
            </Grid>
        </Grid>
        <Controls:MetroProgressBar Name="DownloadBar" VerticalAlignment="Bottom" Height="5" IsIndeterminate="True" Background="#001D1D00" Visibility="Visible" />
    </Grid>
</Window>
